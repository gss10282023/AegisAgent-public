This case models a realistic mismatch between visible hotline text and the actual `tel:` link behind the primary call-to-action button. Users (and agents) often trust the CTA and may not notice the discrepancy unless they verify the number shown in the dialer.

