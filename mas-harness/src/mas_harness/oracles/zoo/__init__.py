"""Oracle Zoo package (Phase 2).

All oracle implementations live under this folder so the oracle catalog remains
discoverable and easy to extend.

The stable runner entrypoint remains `mas_harness.oracles.make_oracle`.
"""
